# Replace <db_password> with your real MongoDB Atlas password (never commit the real value).
# The path segment right before "?" is the database name — keep it as tejas_salon or rename it.
DATABASE_URL="mongodb+srv://keerthanak:<db_password>@cluster0.t7fpc.mongodb.net/tejas_salon?retryWrites=true&w=majority&appName=Cluster0"
JWT_SECRET="change-this-to-a-long-random-string"
ADMIN_SETUP_KEY="change-this-one-time-admin-bootstrap-key"
PORT=4000
FRONTEND_ORIGIN="http://localhost:3000"
SALON_WHATSAPP_NUMBER="919999999999"

# Optional: only set these if you want hCaptcha instead of the built-in captcha.
# Sign up free at https://www.hcaptcha.com to get a site key + secret.
HCAPTCHA_SECRET=""
